import { BaProfilePicturePipe } from './ba-profile-picture.pipe';

describe('BaProfilePicturePipe', () => {
  it('create an instance', () => {
    const pipe = new BaProfilePicturePipe();
    expect(pipe).toBeTruthy();
  });
});
